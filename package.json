{
  "name": "ninja-slicer",
  "version": "1.0.0",
  "description": "A lightning-fast dish slicing game",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251223.0",
    "typescript": "^5.3.3",
    "vite": "^5.4.11",
    "wrangler": "^3.99.0"
  },
  "dependencies": {
    "@cloudflare/kv-asset-handler": "^0.4.1"
  }
}
